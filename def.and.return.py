def plusOne(number):
    return number + 1
newNumber=plusOne(5)
print(plusOne(5))

#summary: you define a new function using the def function and place a parameter
# under that function, the program is to return the parameter with one added to it
# now we call the new plusOne function with an argument of the integer 5 passed to it and set it as a new variable
# we no call the print function on the plusone(5) function
# you get the program to print out the integer "6"
