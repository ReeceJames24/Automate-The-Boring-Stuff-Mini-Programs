def hello(name):   #the name here is called a parameter
    print('hello ' + name)

hello('alice')  #now we pass arguments (the name strings)  when we call the hello function
hello('bob')


#notes: Arguments = the value passed in the function call (like alice and bob strings above)
# Parameters = the variable inside the function (the name variable there at the def function)
    
